seed: 42
Device:
  use_gpu: True
  fp16: False
Dataset:
  path: "datasets/"
  name: MEDMNIST
  params:
    medmnist_flag: PATH
    image_size: 64
    download: True
Training:
  params:
    batch_size: 256
    max_epochs: 1
    learning_rate: 3e-4
    weight_decay: 1e-4
  Pretrain:
    ssl_method: SIMCLR
    augmentations: DEFAULT
    params:
      encoder: "resnet50"
      pretrained: True
      hidden_dim: 512
      output_dim: 128
      temperature: 0.07
      n_views: 2
Logging:
  path: "logs/"
  tool: NONE
  log_steps: 100
